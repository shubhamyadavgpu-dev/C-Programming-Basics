#include <stdio.h>

int main() {
    int age = 18;
    if (age < 5)
    {
        printf("You are under age %d", age);
    }
    else{
        printf("You are old enough %d", age);
    }
    return 0;
}